# Agentic Manufacturing Control Plane · 项目续接文档

更新时间：2026-09-13  
项目目录：D:\mes  
GitHub：https://github.com/kiki720-w/agentic-MES  
功能基线提交：e02c63b  
当前分支：main

## 新对话先读这里

这是一个面向机械加工与装备制造的“制造智能控制层”，不是重新制造一套传统 MES。产品连接客户已有 ERP、MES、WMS、QMS、机床和传感器，在其上形成统一制造数字镜像、有限产能 APS、受控 Agent、审批治理和可追溯回写。

继续开发前必须先读取本文件和 README.md，再执行：

    git status --short
    git log -5 --oneline

不要重新推翻产品定位，不要把演示 MES 恢复成正式产品核心，不要让大模型直接修改生产数据库或控制设备，不要把任何真实 API Key 写入文档、日志、浏览器存储或 Git。

## 已确认的产品定位

1. 产品定位是“连接层 + 制造数字镜像 + APS + Agent 控制层”，客户原有系统继续保存权威业务事实。
2. 早期自建 MES 页面只作为离线模拟器、演示数据源和回归测试夹具保留。
3. Excel / CSV 导入必须保留，作为未接 API 时的正式过渡输入方式；输入先预检、确认和生成可追溯快照。
4. 产品面向通用机械加工和装备制造，不为某一家工厂写死。刀具、夹具、量具、NC 程序、设备、人员与工作单元都使用通用资源语义。
5. 排产思路吸收既有 paichan 项目的实际经验，但不照抄其复杂界面。参考仓库：https://github.com/kiki720-w/paichan
6. 用户首先看到“每个人每天做什么”，再下钻工单、工序、资源负荷、能力缺口和计算依据。
7. 当前 Agent 为 L3 BOUNDED：允许读取、分析、生成和提交排产建议；禁止自批、自发、绕过约束、直接写外部系统数据库或控制 PLC/CNC。
8. L4 是未来目标，不是当前宣传能力。只有真实系统闭环、长期影子验证、可回滚执行和生产级身份体系完成后，才能逐项开放预授权的低风险自动动作。

## 当前系统结构

    ERP / MES / WMS / QMS / 设备 / Excel
                      ↓
        签名连接器、字段映射、来源与版本
                      ↓
              统一制造数字镜像
                      ↓
       确定性有限产能 APS 与质量规则
                      ↓
          L3 虚拟员工「衡策」形成建议
                      ↓
          策略校验、版本冲突与双人审批
                      ↓
               专用连接器受控回写

模型位于解释与交互侧，只读取经过授权和最小化的结构化快照。排产计算、质量评分、权限、审批、发布、设备联锁和最终执行由确定性代码负责。

## 已实现的主要能力

- PostgreSQL 持久化、Alembic 迁移、Outbox、幂等、事件追溯和游标分页。
- 工单、工序、设备遥测、质量、谱系、物料批次、刀具夹具量具及 NC 程序追溯。
- 可治理质量风险策略、历史影响仿真、maker-checker 审批和回滚草稿。
- 统一 APS 快照、有限产能排产、资源能力、标准工时、计划状态机和人工移动排产结果。
- 人员日排程和固定六工作日周计划；大表支持搜索、分页与横向滚动。
- Agent 工作台支持自然语言、XLSX / CSV 预检以及图片和 PDF 附件入口。
- L3 排产 Agent 可自动分析并创建或提交方案，结果标注虚拟员工创建者和提交者。
- 供应商中立模型网关，可热切换 DeepSeek、Kimi、OpenAI 或自定义 OpenAI Chat Completions 兼容接口。
- 模型设置页面：/settings/models。预设服务直接选择，只有自定义服务显示 Base URL 输入。
- API Key 不通过状态接口回显、不进入浏览器存储；连接测试失败不会覆盖当前模型。
- Agent 工作台入口已放入控制塔、排产中心、产能管理和排产结果等主要页面。

## 当前页面

- / ：制造智能控制塔
- /workspace ：制造 Agent 工作台
- /planning ：有限产能排产中心
- /planning/results ：人员排程与全量计划证据
- /capacity ：人员与工作单元产能管理
- /settings/models ：模型供应商与 BYOK 设置
- /simulator ：仅在 simulator mode 开启的旧 MES 模拟器

## 模型与数据安全决策

当前本机通过服务器环境变量配置 DeepSeek，但产品不能绑定单一供应商。正式产品应当把“完全本地、可验证的数据不出厂”作为标准能力，云模型只能是客户主动开启的附加选项。

未来提供三种部署模式：

- LOCAL_ONLY：模型、Embedding、知识库、文件解析、日志和备份全部位于工厂网络；默认拒绝模型网关访问公网。
- PRIVATE_ENHANCED：仍然完全本地，但使用工厂共享的集中式 GPU 推理服务器。
- CLOUD_OPT_IN：只有客户明确授权后才允许访问云模型，不得存在隐藏云端回退。

本地模型硬件结论：

- RTX 5060 Ti 16GB 可以运行适合 MES 日常意图识别、查询、摘要、解释和工具路由的 8B 至 14B 量化模型。
- RTX 5060 Ti 8GB 只适合小模型和开发验证，不推荐作为生产基线。
- 单张 5060 Ti 无法本地运行完整 DeepSeek V4；不要承诺相同模型规模或完全相同效果。
- 工厂不需要每台电脑配置显卡。推荐一台厂内模型服务器供多个客户端共享。
- 控制上下文成本的方法是先由确定性查询和检索提取相关事实，再交给模型，而不是把整个数据库放入提示词。

本地部署尚未完成的关键项：

- 本地模型服务预设与无 API Key 模式。
- LOCAL_ONLY 出站总开关及服务端 URL 策略。
- 本地 Embedding、知识库、文档和图像解析。
- 禁止遥测、隐藏回退和外部日志上报。
- 模型服务健康监测、并发队列、超时和容量指标。
- 可供客户检查的数据出站审计证明。

## 身份与权限现状

当前本机是 DEV 身份模式。页面中的 Demo Supervisor、Demo Planner 和 Demo Quality Manager 可以切换，只用于演示和测试，不能用于试点或生产。

生产要求：

- 使用企业 OIDC / SSO 的签名令牌确定真实 subject。
- 前端不得允许用户把自己切换成 Supervisor。
- 角色和工厂范围由企业身份系统分配，所有权限由服务器校验。
- 敏感操作继续执行 maker-checker，创建者和提交者不得批准自己的方案。
- 生产环境完全隐藏并禁用 DEV 身份选择器。

当前最高优先级仍是生产级身份与权限体系，因为没有可信身份就不能安全开放模型配置、审批或未来 L4。

## 接下来推荐的开发顺序

### 第 40 步：生产身份收口

- 完成控制塔、Agent 工作台、排产和模型设置的一致 OIDC 登录状态。
- DEV 身份切换器只允许 localhost 和显式开发模式。
- 增加会话过期、退出登录、角色不足提示和身份审计测试。

### 第 41 步：本地模型与数据不出厂模式

- 在模型设置中增加“本地模型服务器”。
- 支持厂内 OpenAI 兼容网关和无 Key 服务。
- 增加 LOCAL_ONLY / PRIVATE_ENHANCED / CLOUD_OPT_IN 数据策略。
- LOCAL_ONLY 时服务端拒绝公网模型地址，不允许静默回退云端。
- 增加本地模型健康、延迟、队列和规则回退可视化。

### 第 42 步：模型配置治理

- 供应商、模型、地址和超时持久化。
- API Key 进入客户密钥管理系统或加密密钥库。
- 配置变更审计、版本、测试证据和回滚。
- 配置作用域明确为组织、工厂或工作区，不能由普通用户覆盖。

### 第 43 步：通用连接器中心

- ERP、MES、WMS、QMS 和设备连接配置页面。
- 字段映射、连接测试、同步游标、数据质量和错误重放。
- 不直接写客户数据库，只通过授权 API、消息或边缘适配器交互。

### 第 44 步：补齐高级 APS 约束

- 物料齐套、工序前后关系、班次日历、人员技能和替班。
- 设备维护、刀具夹具占用、换型时间和替代资源。
- 紧急插单、计划冻结区、多目标优化和方案比较。

### 之后：真实工厂影子验证

- 选择第一个真实系统适配器。
- 先只读同步，不回写。
- 连续比较 Agent 与人工排产的准交率、利用率、换型、缺口和计划员工时。
- L3 稳定后才讨论 L4 自动执行范围。

## 当前技术状态

- Python 3.12、FastAPI、PostgreSQL、SQLAlchemy、Alembic。
- 开发 API 地址：http://127.0.0.1:8000
- 当前模型网关：由服务器环境变量决定；不要在续接文档中写入密钥。
- 最近完整回归：92 passed，5 skipped；Ruff 和 mypy 通过。
- 本地 D 盘启动脚本：scripts/start-api-postgres.ps1
- 核心代码：core/src/autonomous_mes
- 关键模型网关：core/src/autonomous_mes/infrastructure/deepseek_gateway.py
- 关键 API 装配：core/src/autonomous_mes/api.py
- APS：core/src/autonomous_mes/application/scheduling.py
- L3 排产 Agent：core/src/autonomous_mes/application/scheduling_agent.py
- 模型设置页：core/src/autonomous_mes/static/model_settings.html
- 最近验收记录：docs/step-34 到 docs/step-39

## 不可破坏的原则

1. 不伪造生产事实，不静默补数据。
2. 不把大模型输出当作生产指令。
3. 不让 Agent 绕过权限、版本、质量、物料、设备或审批约束。
4. 不允许模型直接持有数据库写权限或设备控制权限。
5. 不把 API Key、客户订单、图纸或工艺数据提交到 Git。
6. 保持云端、本地和无模型规则回退三种路径解耦。
7. 所有真实写回必须可审计、幂等、可定位责任人并能安全恢复。

## 给下一对话的启动指令

将下面这段话作为新对话的第一条消息：

> 请继续开发 D:\mes 中的 Agentic Manufacturing Control Plane。先完整阅读 D:\mes\PROJECT_HANDOFF.md 和 D:\mes\README.md，再检查 git status、最近提交及现有测试。继承已经确认的产品定位、安全边界、L3 Agent、供应商中立模型网关和本地数据不出厂方向，不要重新从传统 MES 开始设计，也不要记录或暴露任何 API Key。完成当前最优先且不需要我决策的工作；只有遇到会改变产品方向、真实外部系统选择、身份提供商选择或不可逆操作时再停下来询问。

如果第二个对话不在本机项目上下文中，可以直接发送本文件，或让它从 GitHub main 分支读取 PROJECT_HANDOFF.md。
