# 第九步自然语言生产查询验收记录

日期：2026-09-12  
结论：只读自然语言接口、DeepSeek 实际调用与写意图拦截通过。

## 已实现

- Agent 决策中心新增自然语言生产查询区域和常用问题快捷入口。
- 查询时由 MES 应用服务读取工单、工序、设备和质量检验的当前快照。
- DeepSeek 仅负责根据快照生成中文答案，不获得数据库连接或 MES 操作工具。
- 响应携带模型来源、模型名、策略结论、数据时间和来源对象。
- 模型不可用时返回确定性汇总，并明确标记 `RULES`。
- 命令式复工、放行、派工、审批、删除和控制设备等写意图在模型调用前拒绝。
- 页面使用 `textContent` 展示问答内容，不把模型返回值作为 HTML 执行。

## 实际验收

- 查询“有哪些暂停工单”：DeepSeek 根据 PostgreSQL 当前快照返回工单、工序、设备和报警信息。
- 返回来源：`DEEPSEEK · deepseek-v4-flash · ALLOW_READ_ONLY`。
- 请求“请立即恢复这个工单”：返回 `DENY_WRITE_INTENT`，未执行生产动作。
- 浏览器输入、快捷问题、答案区和来源标签显示正常。
- 全量自动测试：34 项通过。
